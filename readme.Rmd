---
title: tradeR
date: "last update: `r format(Sys.Date(), format = '%d %B %Y')`"
output:
  html_document:
    theme: cosmo
    highlight: tango
---

# Fetch prices
```{r}
system("make -B")
files = list.files(path = "tmp", pattern = "*.json", full.names = TRUE)
print(files)
```

# Let's munge some JSON
```{r}
library("rjson")

for (f in files) {
  print(f)
  raw = fromJSON(file = f)
  
  prices = list()
  for (observation in raw$Data) prices = rbind.data.frame(prices, observation)
  
  t = unlist(prices$time)
  c = unlist(prices$close)
  
  summary(c)
  
  plot(t, c)
  
  # FFT
  f = fft(prices$open - prices$close)
  matplot(sapply(f, abs), pch = 1)
}
```
